# v6.0 实现多种指令，完善 Compile
在v5.0的版本中，一个基本的双向绑定功能基本完成了，在这个基础上完善更多的指令解析就能实现一个比较基础的vue的功能。<br>
在哪里进行更多指令的处理呢？很明显是在 compileElement 函数中。加上对其他指令节点的判断，遍历属性看是否有匹配指令的属性，如果有就对其进行解析编译。<br>
这里添加 v-model 指令和事件指令的解析编译（使用函数compile）作为例子。
